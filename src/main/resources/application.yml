spring:
    datasource:
        url: jdbc:mysql://localhost:3306/portfl
        username: root
        password: root
    jpa:
      hibernate:
        ddl-auto: create
    social:
      facebook:
        appId: YOUR_APP_ID
        appSecret: YOUR_APP_SECRET